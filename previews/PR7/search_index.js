var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Balancing","page":"API","text":"","title":"Balancing"},{"category":"section","location":"api/#Workspace","page":"API","text":"","title":"Workspace"},{"category":"section","location":"api/#Undoing-the-transform","page":"API","text":"","title":"Undoing the transform"},{"category":"section","location":"api/#Extending-to-wrapper-number-types","page":"API","text":"","title":"Extending to wrapper number types"},{"category":"module","location":"api/#PureGebal.PureGebal","page":"API","text":"PureGebal\n\nA pure-Julia, allocation-free matrix balancing routine equivalent to LAPACK's xGEBAL, generic over the matrix element type.\n\nBalancing applies an exact diagonal similarity transform — the scale factors are powers of two, so the transform introduces no rounding error — optionally preceded by a permutation that isolates eigenvalues already available from triangular corners of the matrix. It improves the accuracy of a subsequent eigenvalue computation or matrix exponential.\n\nThe entry points are balance! (in place, allocation-free given a GebalWorkspace), balance (allocating convenience), unbalance! (undo the similarity on a full matrix), and unbalance_eigvecs! (the xGEBAK back-transform for eigenvectors).\n\n\n\n\n\n","title":"PureGebal.PureGebal"},{"category":"function","location":"api/#PureGebal.balance!","page":"API","text":"balance!(A, ws::GebalWorkspace; permute = true, scale = true, check = true) -> ws\n\nBalance the square matrix A in place, writing the transform into ws.\n\nEquivalent to LAPACK's xGEBAL with job = 'B' (or 'P'/'S'/'N' for the other combinations of permute and scale), but written in Julia, generic over the element type, and allocation-free.\n\nArguments\n\nA: square matrix with one-based indexing and unit stride in its first dimension, overwritten with the balanced matrix.\nws: workspace from GebalWorkspace, sized to match A.\n\nKeywords\n\npermute = true: isolate eigenvalues by permuting rows and columns so the matrix acquires triangular corners.\nscale = true: apply a diagonal similarity of exact powers of two making the row and column norms of the remaining block comparable.\ncheck = true: scan A for non-finite entries first, throwing ArgumentError if any is found. Setting check = false skips the O(n^2) scan; a non-finite entry then leaves A untouched and sets ws.info to GEBAL_NONFINITE instead of throwing. Argument validation (dimensions, stride, indexing) always runs and always throws.\n\nReturns\n\nThe mutated ws, whose scale, ilo, ihi, and info fields describe the transform applied to A. Undo it with unbalance! for a matrix, or unbalance_eigvecs! for eigenvectors.\n\nBecause the scale factors are exact powers of two, balancing is an exact similarity transform in floating point: it introduces no rounding error, and the eigenvalues of the balanced matrix are those of the original.\n\nws is mutated, so concurrent balance! calls must not share one. Sharing a workspace across tasks corrupts scale while leaving the balanced matrix itself correct, which silently invalidates any later unbalance!.\n\nExamples\n\nA = [1.0 1.0e6; 1.0e-6 1.0]\nws = GebalWorkspace(A)\nbalance!(A, ws)\n\n\n\n\n\n","title":"PureGebal.balance!"},{"category":"function","location":"api/#PureGebal.balance","page":"API","text":"balance(A; permute = true, scale = true, check = true) -> (B, ws)\n\nAllocating convenience wrapper for balance!: balance a copy of A.\n\nArguments\n\nA: square matrix, not modified.\n\nKeywords\n\nIdentical to balance!.\n\nReturns\n\n(B, ws) where B is the balanced copy of A and ws the GebalWorkspace describing the transform.\n\nExamples\n\nB, ws = balance([1.0 1.0e6; 1.0e-6 1.0])\n\n\n\n\n\n","title":"PureGebal.balance"},{"category":"type","location":"api/#PureGebal.GebalWorkspace","page":"API","text":"GebalWorkspace{R}\n\nCaller-owned workspace for balance!, holding every buffer the routine needs so that balancing allocates nothing.\n\nConstructors\n\nGebalWorkspace(T::Type, n::Integer)\nGebalWorkspace(A::AbstractMatrix)\n\nT is the matrix element type and n the matrix size; the second form takes both from A. R is the real floating-point type underlying T (for a ForwardDiff.Dual element type, the type its derivative information is built on).\n\nFields\n\nscale::Vector{R}: the balancing data, in LAPACK's packed xGEBAL layout — entries ilo:ihi are the diagonal scale factors (exact powers of two) and the entries outside that range are permutation indices.\nilo::Int, ihi::Int: the balanced block is A[ilo:ihi, ilo:ihi]; rows and columns outside it were isolated by the permutation.\ninfo::Int: status of the last balance!; GEBAL_SUCCESS or GEBAL_NONFINITE. See LinearAlgebra.issuccess.\n\nReturns\n\nAn uninitialized GebalWorkspace sized for n-by-n matrices with element type T. Call balance! before reading its fields.\n\nA workspace is mutated by every balance! call, so concurrent calls must each use their own — see balance!.\n\nExamples\n\nA = [1.0 1.0e6; 1.0e-6 1.0]\nws = GebalWorkspace(A)\nbalance!(A, ws)\n\n\n\n\n\n","title":"PureGebal.GebalWorkspace"},{"category":"method","location":"api/#Base.eltype-Union{Tuple{Type{PureGebal.GebalWorkspace{R}}}, Tuple{R}} where R","page":"API","text":"eltype(::Type{GebalWorkspace{R}}) -> Type\n\nThe real floating-point type R the workspace stores its scale factors in — the type underlying the matrix element type it was built for, so a workspace for a ComplexF32 or a ForwardDiff.Dual matrix has element type Float32 or the float its derivative information is built on.\n\nExamples\n\neltype(GebalWorkspace(ComplexF32, 4))\n\n\n\n\n\n","title":"Base.eltype"},{"category":"method","location":"api/#LinearAlgebra.issuccess-Tuple{PureGebal.GebalWorkspace}","page":"API","text":"LinearAlgebra.issuccess(ws::GebalWorkspace) -> Bool\n\nWhether the last balance! into ws succeeded, i.e. whether ws.info is GEBAL_SUCCESS.\n\nExamples\n\nA = [1.0 1.0e6; 1.0e-6 1.0]\nws = GebalWorkspace(A)\nbalance!(A, ws)\nLinearAlgebra.issuccess(ws)\n\n\n\n\n\n","title":"LinearAlgebra.issuccess"},{"category":"constant","location":"api/#PureGebal.GEBAL_SUCCESS","page":"API","text":"GEBAL_SUCCESS\n\nValue of the info field of a GebalWorkspace after a successful balance!. Equal to 0, following LAPACK's info convention.\n\n\n\n\n\n","title":"PureGebal.GEBAL_SUCCESS"},{"category":"constant","location":"api/#PureGebal.GEBAL_NONFINITE","page":"API","text":"GEBAL_NONFINITE\n\nValue of the info field of a GebalWorkspace when balance! was called with check = false on a matrix containing an Inf or NaN.\n\nBalancing is undefined for non-finite input — LAPACK's xGEBAL has no error code for it and its Julia wrapper rejects such input before the call — so the matrix is left untouched and no balancing is performed. Positive info values are Julia-side conditions; negative values would follow LAPACK's \"illegal i-th argument\" convention, but this API validates arguments by throwing instead.\n\n\n\n\n\n","title":"PureGebal.GEBAL_NONFINITE"},{"category":"function","location":"api/#PureGebal.unbalance!","page":"API","text":"unbalance!(X, ws::GebalWorkspace) -> X\n\nUndo, in place, the similarity transform recorded in ws on the square matrix X.\n\nUse this on a matrix function of a balanced matrix: if balance! turned A into B, then unbalance!(f(B), ws) is f(A). For eigenvectors, use unbalance_eigvecs! instead.\n\nArguments\n\nX: square matrix of the same size as the balanced matrix, overwritten.\nws: workspace returned by a successful balance!.\n\nReturns\n\nThe mutated X.\n\nExamples\n\nA = [1.0 1.0e6; 1.0e-6 1.0]\nB = copy(A)\nws = GebalWorkspace(B)\nbalance!(B, ws)\nunbalance!(exp(B), ws)   # == exp(A)\n\n\n\n\n\n","title":"PureGebal.unbalance!"},{"category":"function","location":"api/#PureGebal.unbalance_eigvecs!","page":"API","text":"unbalance_eigvecs!(V, ws::GebalWorkspace; side = :right) -> V\n\nBack-transform eigenvectors of a balanced matrix to eigenvectors of the original, in place. The equivalent of LAPACK's xGEBAK.\n\nArguments\n\nV: matrix whose columns are eigenvectors of the balanced matrix, overwritten with eigenvectors of the original. It must have as many rows as the balanced matrix.\nws: workspace returned by a successful balance!.\n\nKeywords\n\nside = :right: :right for right eigenvectors, :left for left eigenvectors, which scale by the reciprocal factors.\n\nReturns\n\nThe mutated V.\n\nExamples\n\nA = [1.0 1.0e6; 1.0e-6 1.0]\nB = copy(A)\nws = GebalWorkspace(B)\nbalance!(B, ws)\nV = eigen(B).vectors\nunbalance_eigvecs!(V, ws)\n\n\n\n\n\n","title":"PureGebal.unbalance_eigvecs!"},{"category":"function","location":"api/#PureGebal.primalvalue","page":"API","text":"primalvalue(x) -> Number\n\nStrip any derivative or uncertainty information from x, returning the plain number underneath. The extension point through which a number type that wraps a value — a dual number, say — becomes usable with balance!.\n\nBalancing chooses its permutation and its scale factors from primal values alone. That is not an approximation: the factors are exact powers of two and piecewise constant in the matrix entries, so they carry no derivative of their own, and applying them scales a wrapped number's value and its derivative parts identically.\n\nDefined here for Real and Complex. Add a method for a wrapper type to make it work with this package; PureGebal ships one for ForwardDiff.Dual as a package extension. A method must return the wrapped value and must be free of side effects. Implement primaltype alongside it.\n\nReturns\n\nx with all wrapping removed, recursively.\n\nExamples\n\nprimalvalue(1.5)\nprimalvalue(1.0 + 2.0im)\n\n\n\n\n\n","title":"PureGebal.primalvalue"},{"category":"function","location":"api/#PureGebal.primaltype","page":"API","text":"primaltype(::Type{T}) -> Type\n\nThe plain number type underlying T, the type-level counterpart of primalvalue. It determines the element type of a GebalWorkspace's scale vector, so balancing data is stored as plain floating-point numbers rather than as wrapper values.\n\nAdd a method for a wrapper type alongside its primalvalue method.\n\nReturns\n\nThe unwrapped number type, recursively.\n\nExamples\n\nprimaltype(Float64)\nprimaltype(ComplexF32)\n\n\n\n\n\n","title":"PureGebal.primaltype"},{"category":"section","location":"#PureGebal.jl","page":"Home","text":"A pure-Julia, allocation-free matrix balancing routine equivalent to LAPACK's xGEBAL, generic over the matrix element type.\n\nBalancing improves the accuracy of an eigenvalue computation or a matrix exponential. It applies a diagonal similarity transform whose factors are exact powers of two — so the transform introduces no rounding error at all — optionally preceded by a permutation that isolates eigenvalues already readable from triangular corners of the matrix.\n\nLinearAlgebra.LAPACK.gebal! allocates its scale vector on every call, which rules it out of an allocation-free inner loop, and it only accepts the four BLAS element types. PureGebal takes a caller-owned workspace instead, and works for any number type — including BigFloat, Float32, and ForwardDiff.Dual.","title":"PureGebal.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"PureGebal\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using PureGebal: GebalWorkspace, balance!, unbalance!\n\nA = [1.0 1.0e6; 1.0e-6 1.0]\nB = copy(A)\nws = GebalWorkspace(B)\n\nbalance!(B, ws)      # B is now balanced; ws records the transform\nexp_A = unbalance!(exp(B), ws)   # == exp(A), computed more accurately\n\nNothing is exported. Import the names you use, or qualify them as PureGebal.balance!.","title":"Quick start"},{"category":"section","location":"#Reusing-the-workspace","page":"Home","text":"The workspace is the point: build it once, balance many times, allocate nothing.\n\nusing PureGebal: GebalWorkspace, balance!\n\nws = GebalWorkspace(Float64, 100)\nA = randn(100, 100)\n\nfor _ in 1:1000\n    B = copy(A)\n    balance!(B, ws)          # no allocations\nend\n\nA workspace is mutated by every call, so concurrent calls must not share one. Sharing across tasks leaves the balanced matrix correct but corrupts the recorded transform, which then silently invalidates any later unbalance!. Give each task its own.","title":"Reusing the workspace"},{"category":"section","location":"#Element-types","page":"Home","text":"Anything that supports the usual arithmetic works:\n\nusing PureGebal: GebalWorkspace, balance!\n\nA = BigFloat[1 1000000; 1//1000000 1]\nws = GebalWorkspace(A)\nbalance!(A, ws)\n\nNumber types that wrap a value — dual numbers, for instance — work through two small hooks, primalvalue and primaltype. ForwardDiff.Dual is supported out of the box through a package extension:\n\nusing ForwardDiff, PureGebal\n\nfunction balanced_exp(A)\n    B = copy(A)\n    ws = PureGebal.GebalWorkspace(B)\n    PureGebal.balance!(B, ws)\n    return sum(PureGebal.unbalance!(exp(B), ws))\nend\n\nForwardDiff.gradient(balanced_exp, [1.0 1.0e3; 1.0e-3 1.0])\n\nBalancing picks its permutation and scale factors from primal values only. That is not an approximation: the factors are exact powers of two and piecewise constant in the matrix entries, so they carry no derivative of their own, and applying them scales value and partials identically.","title":"Element types"},{"category":"section","location":"#Errors-and-status","page":"Home","text":"Argument problems — a non-square matrix, a workspace of the wrong size, offset indexing — are programming errors and always throw.\n\nNon-finite input is different. Balancing is undefined for it, and LAPACK's xGEBAL has no error code to report it; the stdlib wrapper scans the matrix and throws before calling in. PureGebal does the same by default, and lets you opt out:\n\nusing PureGebal: GebalWorkspace, balance!, GEBAL_NONFINITE\nusing LinearAlgebra: issuccess\n\nA = [1.0 Inf; 0.0 1.0]\nws = GebalWorkspace(A)\n\nbalance!(A, ws; check = false)   # no throw\nissuccess(ws)                    # false\nws.info == GEBAL_NONFINITE       # true, and A is untouched\n\ncheck = false also skips the O(n^2) scan, though that is not why you would reach for it — the scan is a cheap predicate next to the balancing itself, and the two paths measure the same. Its purpose is to remove the throwing branch when the caller wants a status code rather than an exception.","title":"Errors and status"},{"category":"section","location":"#Relationship-to-LAPACK","page":"Home","text":"For the four BLAS element types, PureGebal is intended to be a drop-in replacement for LinearAlgebra.LAPACK.gebal!(job, A), and its test suite asserts bit-agreement on random input.\n\nExact agreement is not guaranteed on every input, and cannot be: GEBAL is a heuristic, and different LAPACK builds disagree with each other. OpenBLAS 0.3.23 and 0.3.29 return different permutations for the same 3×3 matrix. Where implementations differ, each result is a valid balancing — the eigenvalues are preserved exactly either way — so code should not depend on which permutation comes back.\n\nMatrices spanning the full floating-point range are a related case: GEBAL deliberately declines to scale when doing so would overflow, so a large residual imbalance there is correct behaviour, not a defect. PureGebal matches LAPACK's residual on such input.","title":"Relationship to LAPACK"}]
}
